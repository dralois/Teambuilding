using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OverViewListData : MonoBehaviour
{
    private string name;
    private int id;
    private Sprite logo;
    private bool set;

    public void createOverViewListData(bool set)
    {
        this.set = set;
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
